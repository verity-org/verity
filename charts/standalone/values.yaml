# Standalone container images not associated with any Helm chart
# These images are added manually or via the new-image issue workflow
# Format must match Renovate's helm-values manager requirements:
#
# unique-key:
#   image:
#     registry: docker.io  # optional
#     repository: namespace/image
#     tag: "version"

# Example standalone image for testing
busybox-standalone:
  image:
    registry: docker.io
    repository: library/busybox
    tag: "1.36"
