name: busybox
description: "BusyBox Unix utilities — Wolfi rebuild with CVE patches"
upstream:
  package: busybox

types:
  default:
    base: wolfi-base
    packages: ["busybox"]
    entrypoint: /bin/sh

versions:
  "1": {}
