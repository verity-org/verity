name: grafana
description: "Grafana observability platform — Wolfi rebuild targets Debian CVEs from official image"
upstream:
  package: "grafana-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["grafana-{{version}}"]
    entrypoint: /usr/bin/grafana server
    environment:
      GF_PATHS_CONFIG: /etc/grafana/grafana.ini
      GF_PATHS_DATA: /var/lib/grafana
      GF_PATHS_LOGS: /var/log/grafana
      GF_PATHS_PLUGINS: /var/lib/grafana/plugins
    paths:
      - {path: /etc/grafana, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/lib/grafana, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/log/grafana, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

versions:
  "11.6": {}
  "12.4":
    latest: true
