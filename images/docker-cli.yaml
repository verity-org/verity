name: docker-cli
description: "Docker CLI — Wolfi rebuild targets upstream CVEs"
upstream:
  package: docker-cli

types:
  default:
    base: wolfi-base
    packages: ["docker-cli"]
    entrypoint: /usr/bin/docker

versions:
  "27": {}
