name: curl
description: "curl HTTP client — Copa can't patch Alpine-based curlimages/curl; Wolfi rebuild"
upstream:
  package: curl

types:
  default:
    base: wolfi-base
    packages: ["curl"]
    entrypoint: /usr/bin/curl

versions:
  "8":
    latest: true
