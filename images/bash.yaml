name: bash
description: "GNU Bash shell — Wolfi rebuild targets upstream CVEs"
upstream:
  package: bash

types:
  default:
    base: wolfi-base
    packages: ["bash"]
    entrypoint: /bin/bash

versions:
  "5": {}
