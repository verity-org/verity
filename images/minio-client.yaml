name: minio-client
description: "MinIO Client (mc) — Wolfi rebuild targets CVEs from official image"
upstream:
  package: minio-client

types:
  default:
    base: wolfi-base
    packages: ["mc"]
    entrypoint: /usr/bin/mc

versions:
  "RELEASE":
    latest: true
